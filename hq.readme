headquarter是一个大模型后训练系统的集群启动器和资源调度器

负责：
读取用户配置----看看集群有多少台机器----决定每台机器承担什么角色----给各种服务分配gpu---生成启动名利----同时启动这些程序

一次强化学习做两件事情：
1.生成回答-----给模型一批问题，让他生成许多答案

   属于推理，通常：持续时间不固定/不同问题生成长度不同/适合使用sglang这种推理引擎/需要稿吞吐，动态调度和kv cache
2.训练模型-----根据答案的奖励，优势，kl等信息更新参数模型

   属于训练：一批样本-----前向计算-----loss----反向传播---更新参数
   通常：要求严格的数据并行/进行梯度同步/使用megatron等训练框架/对batchsize和各类并行度要求严格

因此试剂系统往往不会让同一个程序同时干所有事情，而是拆开：
sglang推理服务：负责生成回答
actor训练进程：负责更新模型
reference进程：负责提供参考信息

这个脚本就是把它们组织起来

actor是什么：正在被训练的策略模型
例如问题是：请证明勾股定理 actor会产生回答 训练系统会根据回答的质量计算奖励和loss，然后修改actor的权重
所以actor有两种形态：
训练形态：参与前向，反向传播和参数更新
推理形态：被部署到sglang中生成rollout回答

actor训练进程是“学生正在课堂里改正直自己的知识”
rollout服务是把当前学生拉去考试
